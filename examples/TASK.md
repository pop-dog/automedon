# Task

Describe the change you want the coder Workflow to make, in plain prose. The
`code` agent reads this file; the `commit` agent reads it again to write the
commit message. Keep it focused — one coherent change per task.

## Example

Add a `--version` flag to the `orchestrator` binary that prints the crate
version and exits 0. Cover it with a test.
