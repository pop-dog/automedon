Write a commit message for the staged changes shown by `git diff --cached`,
using the task at {{TASK_FILE}} for intent.

Follow this repository's commit-message standards: Conventional Commits, a
capitalized subject line of 50 characters or fewer in the imperative mood,
and body lines wrapped at 72 characters.

Reply with the commit message text only — no git commands, no code fences,
no preamble or explanation.
